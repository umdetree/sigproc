# Compile

```bash
# Linux
gcc libdemod.c -shared -fPIC -o libdemod.so
gcc libscramb.c -shared -fPIC -o libscramb.so

# Windows
gcc libdemod.c -shared -fPIC -o libdemod.dll
gcc libscramb.c -shared -fPIC -o libscramb.dll
```
